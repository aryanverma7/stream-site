import { AdminDashboard } from "@/components/admin/AdminDashboard";

/**
 * The real admin dashboard, replacing the earlier stub. Status and Logs
 * tabs are fully functional; Config and Points are still placeholders,
 * built in a following pass.
 */
export default function AdminPage() {
  return <AdminDashboard />;
}
